{
  "name": "rinlay-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "dev:example": "pnpm --filter example dev",
    "build": "pnpm --filter rinlay build && pnpm --filter create-rinlay build",
    "test": "pnpm --filter rinlay test && pnpm --filter create-rinlay test"
  }
}
